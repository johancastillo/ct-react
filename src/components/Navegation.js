import React, {Component} from 'react';

class Navegation extends Component{
    render(){
        return (
            <h1>
                {this.props.titulo}
            </h1>
        )
    }
}

export default Navegation;